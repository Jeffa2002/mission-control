import assert from 'node:assert/strict'; import test from 'node:test'; import {isSessionAuthorized} from '../../_session-auth-core.ts';
test('live telemetry auth rejects missing and wrong session cookies',()=>{assert.equal(isSessionAuthorized(new Request('http://local/api/agents/live'),'expected-secret'),false);assert.equal(isSessionAuthorized(new Request('http://local/api/agents/live',{headers:{cookie:'mc_auth=wrong-secret'}}),'expected-secret'),false);assert.equal(isSessionAuthorized(new Request('http://local/api/agents/live',{headers:{cookie:'mc_auth=expected-secret'}}),'expected-secret'),true);});
